localnodes: 2

mipengine_images:
  repository: madgik
  version: latest

log_level: DEBUG
framework_log_level: INFO

cdes_path: /opt/mipengine/data
monetdb_storage: /opt/mipengine/db
csvs_datapath: /opt/mipengine/csvs

controller:
  node_registry_update_interval: 20
  celery_tasks_timeout: 10
  nodes_cleanup_interval: 60
  cleanup_file_folder: /opt/cleanup

smpc:
  enabled: false
  optional: false
