import python2monetsql

class Algorithm:

    def algorithm(self, data_table, merged_local_results, parameters, attributes, result_table):
        for iternum in range(60):
            yield python2sql.to_sql(local_step)
            yield python2sql.to_sql(global_step)


    def local_step(self):
        ## execution of a local step
        pass


    def global_step(self):
        ## execution of a global step
        pass
